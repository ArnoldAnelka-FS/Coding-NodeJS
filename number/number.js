const add = (a,b) => a + b;
const mul = (a,b) => a * b;
const div = (a,b) => a / b;
const sub = (a,b) => a - b; 
const max = (a,b) => Math.max(a,b);
const sqrt = (a) => Math.sqrt(a);

module.exports = { add, mul, sub, div, max, sqrt,}